import setuptools

setuptools.setup(
    name="budget_domowy",
    version="1.0",
    packages=setuptools.find_packages(),
    install_requires=['request'],
    description="Aplikacja do prowadzenia budżetu domowego",
    author="Unknown",
    author_email="unknown@gmail.com"
)